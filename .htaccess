# Dar Chatt — custom error pages
# Any URL that does not exist is served /404.html (with the real 404 status).
ErrorDocument 404 /404.html
ErrorDocument 500 /500.html

# Optional hardening: block access to sensitive files
<FilesMatch "^\.env">
    Require all denied
</FilesMatch>
